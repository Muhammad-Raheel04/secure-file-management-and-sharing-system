# Secure File Management & Sharing System
A secure file management API built with Node.js, Express.js, PostgreSQL, and Prisma ORM. It supports JWT authentication, role-based authorization, file uploads, and granular file sharing with read/write permissions

The System is flexible. Admin has all permissions by defualt. Staff can grant others read and write permissions to the files they own. Employee can upload, read and write their files.